import { CalculosSegundaSeccion } from './calculos-segunda-seccion';

describe('CalculosSegundaSeccion', () => {
  it('should create an instance', () => {
    expect(new CalculosSegundaSeccion()).toBeTruthy();
  });
});
